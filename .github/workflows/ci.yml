name: "CI"

on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - "main"

concurrency:
  group: "${{ github.workflow }}-${{ github.head_ref || github.sha }}"
  cancel-in-progress: true

env:
  FLOX_DISABLE_METRICS: "true"

jobs:

  test-action:
    name: "Github Action Test"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    strategy:
      matrix:
        os:
          - "ubuntu-latest"
          - "macos-latest"
        channel:
          - "stable"
          - "nightly"

    steps:
      - name: "Checkout"
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6


      - name: "Install Flox"
        uses: flox/install-flox-action@f6002ed63e483f134001de7b4b45be891e00b09f # main

        with:
          channel: "${{ matrix.channel }}"

      - name: "Test Remote environment"
        uses: "./"
        with:
          environment: "flox/python-pip"
          command: "python --version"

      - name: "Test Local Action"
        uses: "./"
        with:
          command: "python --version"
          dir: "./test-env"
